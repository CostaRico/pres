import Ember from 'ember';

export default Ember.Component.extend({
  didRender(){
    /*let height = document.getElementById('vertical-img').height;
    debugger;
    let mapEl = document.querySelectorAll('#building-view-map .map__container')[0];
    mapEl.offsetHeight = height;*/
  }
});
